/**
 * 测试分支结构的使用2：if-else结构的使用
 * @author 张家懿
 */

#include <stdio.h>

int main(int argc, char const *argv[]) {
    int x;
    scanf ("%d" , &x);
    if (x>=0)  
        printf ("%d", x);
    else  
        printf("%d", -x);
    return 0;
}
