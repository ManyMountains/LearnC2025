/**
 * 习题：判断某年是不是闰年
 */

#include <stdio.h>

int main(){
    //判断某一年是否是闰年：
    //1. 能被4整除且不能被100整除的年份是闰年
    //2. 能被400整除的年份是闰年
    int n;
	scanf("%d",&n);
	if((n%4==0&&n%100!=0)||(n%400==0))
	    printf("是闰年");
	else
	    printf("不是闰年");
	return 0;
} 
